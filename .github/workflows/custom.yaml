name: CoderCo Custom Actions

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest


  steps:
    - name: checkout code
      uses: actions/checkout@v2


    - name: hello world custom actions
      uses: musti579/Coderco-custom@main
      with:
        who-to-greet: "CoderCo team"






